<?php

    // $price = 5;
    // $tax = 0.075;

    // 小数点第2位まで表示
    // printf("The dish costs $%.2f", $price * (1 + $tax));

    // $zip = '6520';
    // $month = 2;
    // $day = 6;
    // $year = 2017;
    //
    // printf("ZIP is %05d, and the date is %02d/%02d/%d\n", $zip, $month, $day, $year);

    $min = -40;
    $max = 40;

    printf("The computer can operate %+d and %+d degrees Celsius.\n", $min, $max);

?>
